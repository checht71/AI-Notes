A flowchart with reward values and probabilities attached to each state.

You can map out a whole Markov Decision Process (MDP) using a [[State Transition Matrix]].
You can also use Q, V and A functions on the MDP.