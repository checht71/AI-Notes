All of reinforcement learning falls under the umbrella of self-supervised learning. This is because the AI we use is never shown the correct/optimal answer to the problems it works on. It tests and empirically updates itself using probability algorithms like Bayes Theorem. That's why they said in Prob and Stat that Bayes Theorem is huge in machine learning. That counts as the "loss function" of reinforcement learning AI.